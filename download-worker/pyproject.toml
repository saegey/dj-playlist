[project]
name = "download-worker"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "redis==5.0.1",
    "yt-dlp==2026.6.9",
    "gamdl==2.6.5",
    "requests==2.33.0",
    "psycopg2-binary>=2.9.9,<2.10",
    "groovenet-python-client",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-mock>=3.14",
    "fakeredis>=2.20",
]

[tool.uv.sources]
groovenet-python-client = { path = "../packages/groovenet-python", editable = true }

[tool.pytest.ini_options]
testpaths = ["tests"]
