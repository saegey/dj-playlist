services:
  app:
    ports:
      - "127.0.0.1:${APP_PORT:-3000}:3000"
    volumes:
      - ${APP_AUDIO_MOUNT:-worktree_audio:/app/audio}

  download-worker:
    volumes:
      - ${WORKER_AUDIO_MOUNT:-worktree_audio:/app/audio}

volumes:
  worktree_audio:
